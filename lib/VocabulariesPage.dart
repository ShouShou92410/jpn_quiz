import 'package:flutter/material.dart';

class VocabulariesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {

    return Scaffold(
      appBar: AppBar(
        title: Text('Vocabularies'),
      ),
      body: Center(child: Text('Vocabularies'))
    );
  }
}